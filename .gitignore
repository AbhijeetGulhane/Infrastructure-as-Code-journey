cat <<EOF > .gitignore
# Terraform local state and plugins
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl

# Sensitive variables (if you ever use them)
*.tfvars
*.tfvars.json

# Operating system files
.DS_Store
Thumbs.db
EOF